{
  "name": "portfolio-chatbot-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ../scripts/demo-build.js && wrangler dev",
    "deploy": "bash ../scripts/build-and-deploy.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo:build": "node ../scripts/demo-build.js",
    "demo:password": "node ../scripts/demo-build.js --password"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0"
  }
}
